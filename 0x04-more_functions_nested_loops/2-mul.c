#include "main.h"

/**
 * mul - products of two integers
 * @c: the first integer
 * @d: the second integer
 * Return: result multiplication of both integers
 */

int mul(int c, int d)
{
	return (c * d);
}

#include "main.h"

/**
 * _islower - checks for lowercase alphabets
 * @c: given alphabet or character
 * Return: 1 if c is lowercase, 0 if otherwise
 */

int _islower(int c)
{

	return (c >= 'a' && c <= 'z');

}
